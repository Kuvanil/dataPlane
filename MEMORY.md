# MEMORY — Cross-Session Project State

This is a working log for agents (Kimchi and others) that don't retain context between sessions. Read the **Current state** section and the last 3-5 **Log** entries before starting work; append one short entry when you finish a task. This is not a changelog for humans — `git log` is authoritative for that. Keep entries short and factual; prune stale ones instead of letting this file grow unbounded (target: keep it under ~150 lines — archive older log entries into `requirements-specs/**/INDEX.md` progress logs where they already live, don't duplicate).

## Current state (as of 2026-07-06)

- **Stack:** FastAPI + Celery + Postgres backend, Next.js (App Router, React 19) frontend, Docker-first. No migration tool — schema changes apply via `Base.metadata.create_all` on app start (see `SKILLS.md` §2).
- **Core platform capabilities shipped:** Audit Trail, Schema Drift Detection (Celery beat), persistent Query History/Chat Sessions, JWT auth, AI Autopilot backend (7-step agent loop). See `backend/app/models/` and `backend/app/api/routers/` for the concrete surfaces.
- **Schema Mapper TRD gap-closure** (`requirements-specs/mapper_tasks/INDEX.md`): tasks #1 (N:1 mapping UI), #3 (nullability display), #5 (autosave-loss warning), #6 (rename UI) are done. #2 (keyboard a11y for drag-and-drop) and #4 (canvas virtualization) are open — both need a human decision (accessibility audit; virtualization library choice) before auto-implementing. #7 (tenant isolation sign-off) is blocked on product.
- **Pipelines epic** (`requirements-specs/Pipelines_tasks/INDEX.md`): tasks defined (data model, drift validation, execution engine, scheduler, retry handling, run history, UI monitoring, audit/role gating, concurrency, tests, secret vaulting) — check that INDEX.md for current status before assuming any are done.
- **Governance files added 2026-07-06:** root `CLAUDE.md` (entrypoint), `SKILLS.md` (task playbooks), this file. `prompts/*.md` remains the binding coding-standards contract and was intentionally left unmodified in that pass.
- **Dashboard epic** (`requirements-specs/dashboard_tasks/INDEX.md`): tasks #1–#7 done 2026-07-07 (aggregation API + cache + role-scoping + widget framework + tiles/feed/range-filter); #8 backend tests done, frontend component tests blocked on missing jest/RTL infra (human call); #9 tenant sign-off blocked on product.
- **Connectors epic** (`requirements-specs/connector_tasks/INDEX.md`): tasks #1, #3–#7 done 2026-07-07 (soft-delete data model + ConnectionService, types catalog + config validation, test diagnostics w/ 5s timeout across all 5 drivers, beat health checks, discovery handoff, dependency-aware delete/restore/hard-delete; plus a hotfix — connector reads were unauthenticated and returned plaintext passwords; now auth-gated + secret-redacting). #2 (secret vaulting) and #8 (edit/rotate) blocked on encryption-approach sign-off; #10 tenant sign-off blocked on product. 6/9 FRs done.

## Known constraints / gotchas

- `SECRET_KEY` (JWT) must be overridden via env var in production — code default is dev-only.
- No Alembic-equivalent migrations — destructive schema changes (drop/rename column) need a human sign-off, not an auto-implementation.
- Celery beat requires its own `beat` service entry in `docker-compose.yml` — periodic tasks silently won't fire without it.
- `frontend/src/lib/api.ts` is the single chokepoint for HTTP calls and 401 handling (`setUnauthorizedHandler`) — don't add a parallel fetch or auth-redirect path.

## Log

- 2026-07-14 — Scoped (spec only, no code) a new epic to merge AskData Bot and Query Studio —
  both already fully built as separate sidebar tabs (8/9 FRs each) — into one dashboard tab with
  an Ask/SQL mode toggle. `requirements-specs-v2/query_workspace_tasks/INDEX.md` + 8 task files
  (shared shell keeping both subviews mounted, single connection source of truth, replacing the
  existing `sessionStorage["qs-handoff"]` + hard-navigation link between them with an in-shell
  callback, write-confirm-modal-survives-mode-switch guardrail, keyboard-shortcut scoping,
  sidebar/route consolidation with redirects for the old `/dashboard/askdata` and
  `/dashboard/query-studio` paths, an audit-trail label-clarity check, and test consolidation).
  Extended same day per follow-up: added tasks #9-#12 for "Investigate in Query Workspace"
  handoffs from Schema Intel (drift events, PII/classification badges) and Schema Mapper (AI
  suggestions, validation issues) — a new, generalized `sessionStorage`-based `WorkspaceHandoff`
  contract (distinct from the #1-#8 in-shell callback, since Schema Intel/Mapper stay separate
  routes). Confirmed via audit: neither feature has any existing outbound cross-tab link today,
  and neither TRD specifies this as an FR — net-new UX. 12 tasks total, not started.
- 2026-07-14 (same day, later) — Query Workspace epic built (by a separate session) and then
  reviewed + fixed in this pass — full detail in
  `requirements-specs-v2/query_workspace_tasks/INDEX.md`'s progress log,
  `bugs.md`/`enhancements.md` in that same directory. New: `/dashboard/query-workspace`
  (`QueryWorkspaceInner.tsx` + `AskDataView`/`SqlWorkspaceView` subviews, both kept permanently
  mounted and toggled via CSS so state survives a mode switch), `handoff.ts`'s
  `WorkspaceHandoff`/`writeWorkspaceHandoff`/`readAndClearWorkspaceHandoff`, "Investigate →"
  actions wired into Schema Intel (`DriftHistoryPanel`, `CatalogTableCard`) and Schema Mapper
  (`SuggestionPanel`, `ValidationPanel`), old `/dashboard/askdata` + `/dashboard/query-studio`
  routes replaced with thin redirects. Review found and fixed 5 real bugs before calling it done:
  a hardcoded placeholder query (`SELECT * FROM related_table`) on one Investigate action — a
  genuine no-mock-UI violation, not just a style nit; a half-wired background-completion badge
  that could never fire for AskData (SQL side worked, Ask side didn't); dead
  `sessionStorage["qs-handoff"]` + hard-navigation code left in `ChatBubble.tsx` that task #3 had
  explicitly said to delete; a shipped SQL-handoff mechanism (ref + `setTimeout(...,0)`) that
  wasn't the one its own tests exercised; and, caught only by running `npm run lint` (not manual
  review) — this repo's ESLint config already enforces the newer `react-hooks/refs` /
  `react-hooks/set-state-in-effect` rules — a shell-level modal reading `ref.current` during
  render and a mount-only effect doing 5 setState calls instead of using a lazy `useState`
  initializer. **Lesson worth repeating for future epics: always run the project's actual lint
  config as part of "done," not just tsc/build/tests** — it caught two real bugs neither manual
  review nor the test suite would have. Also added the test coverage the epic's own tasks #8/#12
  asked for but hadn't landed (0 → 106 vitest tests passing for this epic's code, up from having
  none for the shell itself or any of the four investigate actions). Verified: `tsc --noEmit`
  clean, `npm run lint` clean for every file touched, `npm run build` clean (19/19 routes),
  `npx vitest run` 106/106. Not yet exercised against a live backend/browser — no Docker/browser
  automation available this pass. **Uncommitted.**
- 2026-07-09 — Resolved the decision-blocked items surfaced by the bug-validation sweep, per user choice ("just resolve the decisions, implement what's unblocked"): (1) **Mapper #02 keyboard a11y** — `Canvas.tsx`'s existing click-to-stage/click-to-connect flow (built for N:1 mapping) is now keyboard-operable: actionable rows get `tabIndex=0`/`role=button`/`aria-pressed`/`aria-label`, Enter/Space fire the same `activate()` as click, plus a visually-hidden `aria-live` region announcing staging/connection events. Reused the existing mechanism rather than inventing a new interaction model. (2) **Mapper #04 virtualization** — added `react-window` (`FixedSizeList`), which forced a real architectural fix alongside it: the connector-line SVG overlay assumed absolute unscrolled row positions, so virtualizing without more would have silently misaligned connector lines the moment a panel scrolled. Fixed by tracking each panel's scrollTop and having the overlay hide (never mis-draw) a connector whose endpoint has scrolled out of view. Below the new bounded-height cap (560px), rendering is pixel-identical to before — only large schemas actually virtualize. 7 new tests in `schema-mapper/__tests__/Canvas.test.tsx`. (3) **Schema Intel #05 classification confidence** — presented 3 options, user chose defer entirely (real confidence needs profiling/FR2 first; a rule-based interim score would be a second path to retire later — not worth it). Documented, no code. (4) **Tenant isolation** — user chose "draft the architecture decision + epic spec, no code." Wrote `requirements-specs/tenant_isolation_tasks/00_architecture_decision.md`: recommends row-level `tenant_id` + Postgres RLS (not app-filtering alone — a missed WHERE clause is a real leak) over schema-per-tenant or DB-per-tenant, full 11-root/13-child table inventory, data model + auth sketch, 5 open questions for Security/Product (tenant cardinality per user, physical-isolation requirement, platform-superadmin bypass, rollout sequencing, legacy-data backfill). This is the **first substantive proposal** after six prior epics (mapper #7, schema_intel #9, connector #10, dashboard #9, autopilot #11, plus the original `review_schema_mapper_tasks/CONTRADICTIONS.md` C4) all independently flagged the same gap and said "wait for a decision" — updated all six to point here instead of restating the finding a seventh time. Also noted: `Pipeline.tenant_id` (a nullable `String` placeholder added speculatively during the pipelines build) needs retyping to a real FK once this lands. Verified: frontend tsc/build clean, vitest 70/70 (17 new), lint at 29-problem baseline; backend untouched this round (docs + frontend only). Live: frontend container rebuilt/deployed; no browser-automation tooling (Playwright/chromium-cli) available in this environment, so the interactive keyboard/virtualization behavior was verified via RTL's real-DOM/real-event simulation rather than an actual browser screenshot — flagged explicitly rather than overclaimed. **Uncommitted.**
- 2026-07-09 — Dashboard bugs round 2: fixed #02, #03, #05 from `dashboard_tasks/bugs/INDEX.md`; reviewed #01 and left it by design (see that INDEX's disposition note — accepted per-process cache tradeoff, not a defect); #04 already confirmed stale/fixed in the round-1 pass. Frontend-only changes: `useWidgetData` fetchers now take `(signal: AbortSignal)` and the hook actually cancels a superseded/unmounted request instead of just discarding its result (`api.get` gained an optional `{ signal }` param); `setUnauthorizedHandler`'s single nullable slot replaced with `addUnauthorizedHandler(fn) -> unregister` backed by a `Set`, so a second concurrent registrant can't clobber the first (only caller, `useMapping.ts`, migrated — no shim kept); new `page.test.tsx` (9 tests) closes the real gap in #05 — every dashboard leaf component already had tests, `page.tsx`'s composition/wiring logic (237 lines) didn't. New `src/lib/__tests__/api.test.ts`. Verified: tsc clean, vitest 63/63 (16 new), lint at 29-problem baseline, build clean. Backend untouched this round. **Uncommitted.**
- 2026-07-09 — Time-boxed review pass across the newly-added `requirements-specs/*/bugs/` validation reports (autopilot, connectors, dashboard, mapper, pipelines, schema_intel — one per epic). Fixed the one live, actionable, previously-under-scoped item found: connector_tasks bug #03 (SQL injection via f-string table-name interpolation) was filed against Postgres only, but the same pattern existed in `mysql.py` (pointless f-string, no interpolated value) and `sqlite.py`/`oracle.py` (`PRAGMA table_info({table_name})` / `PRAGMA foreign_key_list({table_name})`, which can't take bound params — fixed via identifier quoting instead). All 4 connectors fixed; suite re-run 335/335 (no new test — no reachable malicious-input path since table names only ever come from `get_tables()`, fixed as defense-in-depth per the bug's own "high in principle" framing). Also found dashboard_tasks bug #04 ("per-module rollback missing") to be a **stale report** — the rollback was already added in the 2026-07-07 build; corrected the record rather than re-fixing already-fixed code. Left untouched, correctly: pipelines bugs #12–19 (already fixed pre-existing), mapper #02/#04 ([?] need human input) and #07 ([!] blocked), schema_intel #04/#06/#07/#08 (unbuilt features, not bugs) and #05 ([?]), dashboard #01/#02/#03/#05 (LOW, legitimate backlog, not broken behavior) — none of these are "fix in 10 minutes" scope.
- 2026-07-09 — Autopilot bug round: 5 validated findings in `requirements-specs/ai_autopilot_tasks/bugs/`, all fixed + 6 regression tests (suite 335/335, autopilot 71/71). Fixes: dispatch-after-commit pattern for the migration executor (executors NEVER commit the caller's session — reserved `DISPATCH_AFTER_COMMIT_KEY` callable fired after the owning commit); engine sweep skips+counts invalid drafts instead of dying; refresh/expire after every bulk `.update(synchronize_session=False)`; newest-drift-per-connection via SQL `group_by`+`max(id)` instead of loading the window into Python; structured `blocked_by` key in block details/audit payloads. **`bugs/notes.md` is the reusable lessons doc — it ends with a pre-flight checklist to copy into the next epic's INDEX.** Third consecutive epic where post-completion review found real bugs at 100% test-pass — treat post-epic review as a standing phase. Known accepted deviations documented in notes.md (request_suggestions' internal commit; no auto-recovery for crash-stranded `executing` recs). Images rebuilt + live smoke passed.
- 2026-07-11 — Audit Trail epic (`requirements-specs/audit_trail_tasks/`) tasks #1–#6 done in one day, full detail in that INDEX's progress log. Started from a prior uncommitted session's commit (`34fb9e0`) that had written a lot of code (canonical schema, hash-chain scaffolding, ingestion/search/export routers) but left the **whole backend import-broken** (`AuditLog.metadata = Column(...)` collides with SQLAlchemy's reserved `Base.metadata` — fixed by renaming to `event_metadata`) and completely untested. Testing that code surfaced 3 more real bugs, not just the import break: (1) the hash chain used flush-then-mutate-then-UPDATE, incompatible with a true append-only trigger — refactored to precompute sequence/hash before a single INSERT; (2) hashing `created_at` was flaky because SQLite doesn't preserve tzinfo through a session-expire re-fetch, so `verify_hash_chain` false-flagged every row — dropped timestamp from the hashed content; (3) export's "streaming" loaded the full result set into memory, *and* a naive fix (lazy-query inside the StreamingResponse generator using the request's `Depends(get_db)` session) would have broken silently — FastAPI closes that session as soon as the endpoint returns, before the generator actually iterates — confirmed via a standalone repro before trusting the fix; solved with a dedicated session scoped to the generator's own lifetime. Also added: DB-level append-only trigger (`app/core/audit_guard.py`, Postgres PL/pgSQL + SQLite, installed at startup) verified against real Postgres via `psql UPDATE` → rejected; circuit-breaker + bounded in-process buffer + Celery flush task for ingestion durability (reused the existing `app/core/circuit_breaker.py` pattern from Ollama); composite indexes + a `sort_by` allow-list (unvalidated `getattr` would resolve to `Base.metadata` again and 500); full frontend viewer (`dashboard/audit/` — table/filters/detail/correlation-timeline/export, new `api.download()` in `lib/api.ts`). Verified: backend pytest 442/442, frontend tsc/build/vitest 76/76 clean, lint zero new problems, live smoke against rebuilt `api`/`worker`/`beat`/`frontend` containers (they were crash-looping the whole session on the pre-fix image). **Open:** #7 (retention + role gating — audit router has zero auth today) and #9 (security sign-off) not attempted. **Uncommitted.**
- 2026-07-11 — Query Studio epic (`requirements-specs/query_studio_tasks/`) tasks #1–#6, #8 built end-to-end same day as Audit Trail, full detail in that INDEX's progress log. 8/9 FRs done — only FR7 (send-to-Visualize) is open, and it's a **product decision**, not a build gap: the TRD wants query results sent to Visualize for charting, but `/dashboard/visualize` is actually a ReactFlow schema-relationship graph (tables/PII risk), not a chart surface — same category of TRD-vs-reality mismatch AskData Bot's INDEX already flagged for its own module. New: `query_execution_service.py` (wires in a previously-built-but-unused `statement_classifier.py` — its `sqlparse` import wasn't even in `requirements.txt`, would have crashed in prod on first use), `SavedQuery` model, `/api/v1/query-studio` router (execute/history/saved/export), gated with the existing `require_role("admin"|"analyst")` dependency — first auth on any Query Studio surface. Reads execute through the existing connector `execute_query()` path; **writes go through a separate cursor + explicit `commit()`** — reusing the read path for a write would have silently rolled it back on connection close, since `execute_query()` was only ever exercised read-only before (verified this distinction matters with a real SQLite-file test: re-query after connection close proves the commit actually happened). Write/DDL execution requires admin role **and** explicit `confirm=true`; multi-statement input is rejected outright (stacked-statement write ambiguity). History reuses the Audit Trail infra from earlier this session (`module=query_studio` audit rows) instead of a new table. Frontend: replaced the OLD `/dashboard/query-studio` page, which — per that INDEX's original audit — was actually mislabeled AskData-style NL2SQL chat calling `/api/v1/query/nl2sql`, with a real SQL editor (new deps `@uiw/react-codemirror` + `@codemirror/lang-sql`, schema-aware autocomplete from the existing Schema Intel catalog endpoint, write-confirm modal, history/saved-queries sidebar, CSV export). Verified: backend pytest 458/458 (up from 442), frontend tsc/build clean, lint zero new, vitest 81/81 (up from 76), live curl smoke against the real containers (list-tables SELECT, blocked-then-confirmed write, saved-query CRUD, CSV export all exercised against the seeded SQLite connections). **Test-authoring gotcha worth remembering:** two `client_*` TestClient fixtures used simultaneously in one test both mutate the same global `app.dependency_overrides` dict — the second one silently wins for every request regardless of which client variable calls it, silently defeating "test as two different users" tests. Fixed with a `switch_user()` helper that swaps identity sequentially instead of stacking fixtures. **Also observed (pre-existing, not caused by this change, not fixed):** adding a new table makes one of gunicorn's two boot workers lose a `create_all()` race against the other — harmless (container ends up healthy) but will recur for every future schema addition given this repo's no-migration-tool setup. **Open:** FR7 blocked on product decision; #10 security sign-off not attempted (this is the first surface in the app that lets an authenticated user execute arbitrary write/DDL SQL against a connected database — real review needed before production, not just green tests). **Uncommitted.**
- 2026-07-11 — AskData Bot epic (`requirements-specs/askdata_bot_tasks/`) tasks #1–#5, #7 done same day as Query Studio (which this depends on for its execution path), full detail in that INDEX's progress log. 8/9 FRs done — only FR5's "visualize" sub-part is open, same product-decision block as Query Studio's FR7. Realigned `/dashboard/askdata` from a canned-response "database intelligence" chat into the actual conversational NL-to-SQL bot the TRD wanted: new `askdata_pipeline_service.py` grounds generation in the Schema Intel catalog (falls back to live schema + flags `grounded: false` if unscanned), reuses `NL2SQLService.generate_sql` (shared with legacy `query.py`, not forked) and Query Studio's `statement_classifier` (refuses anything not a plain SELECT — AskData never writes, unlike Query Studio), masks PII columns for the `viewer` role via the existing keyword-based `SecurityService` classifier. Conversation context folds into the question text itself (`generate_sql` has no history param) — documented tradeoff: the fast-path template matcher also sees history-prefixed text, so a prior turn containing an exact template phrase could misfire a later one (judged low-risk). Extended `ChatMessage` with nullable `connection_id`/`sql_text`/`row_count` instead of a new table — another **additive change to an already-populated live table**, so the dev Postgres needed a manual `ALTER TABLE` (same gotcha class the connectors epic hit first). New `POST /askdata/ask` + `GET /askdata/sessions/{id}/messages`; old `/chat`/`/nl2sql`/`/suggestions` left as unreferenced dead code rather than deleted (nothing else calls them, but deleting wasn't asked for). Frontend: real chat UI (connection picker, collapsible SQL per turn, result table, masked-columns notice, "Edit in Query Studio →" handoff writing to `sessionStorage` — Query Studio's page gained the consuming side). Verified: backend pytest 467/467 (up from 458), frontend tsc/build clean, vitest 87/87 (up from 81), and a live smoke test that actually round-tripped through the real Ollama container (`method: "llm"`) — scanned a connection, asked a grounded question, ran a follow-up in-session, created a throwaway viewer-role user and confirmed PII columns came back redacted for them but not for admin, confirmed the audit trail recorded it, then deleted the test user. **Open:** FR5 visualize sub-part (product decision), #9 security sign-off (PII masking here rides on Schema Intel's still-partial classifier — real review needed before this touches anything sensitive). **Uncommitted.**
- 2026-07-08 — AI Autopilot governance epic built end-to-end (`requirements-specs/ai_autopilot_tasks/`, tasks #01–#10; #11 sign-off blocked on human). **Critical hotfix folded in: the legacy autopilot router was fully unauthenticated — including `mode="execute"` which copies data into the target** (same defect class as the connectors hotfix). New: policy/recommendation/action-log models, `autopilot_registry.py` (4-action allow-list + PROHIBITED set + default-deny, auto only if reversible+low-risk, asserted at import), `autopilot_service.py` (guarded-UPDATE status machine; bounded executor: prohibited hard-block → policy/breaker/rate-limit clamps → execute; demote-to-queue fail-safe), `autopilot_engine.py` (drift + connector-health evaluators, deterministic metadata-grounded rationale — deliberately NO LLM on the decision path per TRD §10 prompt-injection risk), `autopilot_tasks.py` (2-min beat + executor task, inline evaluate hooks in drift/health tasks for the ≤10s NFR). Legacy execute-mode now returns `queued_for_approval`. Frontend: autopilot page → 4 tabs (Run console/Approvals/Policy/Action log). Verified: pytest 329/329 (65 new incl. AC1–AC4 safety suite), vitest 46/46 (11 new), tsc/build clean, lint 29 baseline, images rebuilt, full live pass (401 gate, 422 auto-clamp, queue→approve→execute→audit-order, beat firing observed). **Uncommitted.** Gotchas: (1) `AutopilotRecommendation` dedupe_key mirrors the mapper zombie-suggestion lesson — open recs refresh in place, cleared triggers supersede; (2) executor dispatches Celery AFTER committing the rec (worker session can't see uncommitted rows — bit me in the migration executor's FK); (3) circuit breaker computes from the action log at execution time, never mutates policy rows.

- 2026-07-06 — Added `CLAUDE.md`, `SKILLS.md`, `MEMORY.md` at repo root to formalize agentic-SDLC practice on top of `prompts/*.md`, and wrote instructions in a compact, section-scoped style for Kimchi's limited context window. `prompts/*.md` left as-is per decision to treat it as already current.
- 2026-07-06 — Code review of pipelines commit `3866c7e` (Tasks #1+#2) filed and **fixed** bugs #12–#19 (`requirements-specs/Pipelines_tasks/1*.md`). Highlights: the Task #1 refactor had silently destroyed the legacy `POST /execute` executor (restored verbatim + regression tests); the Pipelines INDEX had marked spec-only tasks as done FRs (corrected — real status is 2/10 FRs done); drift check hardened (fail-closed on missing snapshot, column-order-normalized hashing, column-level `changed_tables`, mapping↔connection match enforced at create). Suite: 164/164. Lesson recorded in INDEX legend: `[x]` = landed tested code only, never "spec written". Uncommitted — working tree also holds unrelated staged schema-catalog work.
- 2026-07-06 — User report "schema mapper always shows same tables / no dropdown / static" traced to (a) the **Visualize** tab auto-picking hardcoded connector ids 1/2 with unlabeled selects, and (b) the **dashboard home** being mostly string literals (tiles, activity feed, connection-health %s — only drift alerts were real). Filed + fixed 5 tasks in `requirements-specs/dashboard_static_ui_tasks/INDEX.md`: Visualize now defaults to first-two connectors with labeled 📤/📥 pickers and a proper empty state (was: infinite spinner when <2 connectors); home tiles/feed/health now driven by `GET /connectors/`, `POST /connectors/{id}/test`, `GET /audit/`, `GET /audit/summary`, `GET /mappings/` — unbacked numbers ("Total Tables 15", "AI Matches 92%", "PII 8", health %s) removed per the no-mock-UI rule. tsc/lint/build clean (lint = 30-problem baseline, zero new), frontend image rebuilt + smoke-checked live. **Uncommitted.** Gotcha for next time: seeded demo data uses connector ids 1/2 — magic-id assumptions hide in `query-studio/page.tsx` and `askdata` too (not audited this pass).
- 2026-07-06 — Review round 2 of the schema-mapper epic (same treatment as pipelines): 12 findings filed and **all fixed** (`requirements-specs/review_schema_mapper_round2/INDEX.md` has the full per-bug log). Notable: two HIGHs were regressions introduced by round-1 fixes themselves — the one-at-a-time autosave drain reintroduced a concurrency race the old `splice(0)` was immune to (fixed with a ref-based in-flight guard; state-based guards are useless inside mount-once-effect closures), and the N:1 staging set survived mapping switches. Backend gained `uq_field_target_per_draft` partial unique index (drafts weren't covered by `uq_field_target_per_version` — NULL version_ids are distinct). Verification: pytest 167/167 (3 new), tsc/build clean, lint byte-identical to HEAD baseline. **Uncommitted.** Lesson: post-review fix batches need their own review pass — both HIGHs came from the "fixes", not the original features.
- 2026-07-07 — Dashboard epic built (`requirements-specs/dashboard_tasks/`, tasks #1–#7 + #8-backend; full detail in that INDEX's progress log). New: `GET /api/v1/dashboard/summary?range=` (8 KPI tiles + enriched feed, per-module failure isolation, per-user 30s TTLCache, viewer role masking), frontend `DashboardWidget`/`KPITile`/`ActivityFeed`/`TimeRangeFilter` + `useWidgetData`, `page.tsx` rewired to the aggregation API. New dep `cachetools`. Verified: pytest 191/191 (24 new), lint at 30-problem baseline, api+frontend images rebuilt and endpoint exercised live. **Uncommitted.** Gotchas worth remembering: (1) per-module try/except MUST `db.rollback()` in each handler or one failed query poisons the rest of the session; (2) task-spec pseudocode drifted from real models (`Pipeline` has no `status`, `DBConnection` has no `is_deleted`) — always re-ground specs against `app/models/` before implementing; (3) frontend has NO jest/RTL infra — dashboard spec assumed it exists, component tests need a human decision.
- 2026-07-07 — Dashboard follow-up (user-approved): added the repo's first frontend test infra — Vitest + RTL per the Next-bundled vitest guide (`frontend/vitest.config.mts`, `vitest.setup.ts`, `npm test`); 35 component/hook tests in `src/app/dashboard/__tests__/`, all green; dashboard_tasks #8 now fully [x]. Backend dashboard coverage measured at 90% (pytest-cov in venv only, not pinned). Seeded idempotent demo data via api container (tag: actor/created_by `demo-seed`) so all 8 KPI tiles show range-scoped values — dev Postgres only, delete by that tag. **Uncommitted.** Gotcha: with React 19, `act(() => someAsyncFn())` without await corrupts the act scope and nulls `result.current` in LATER tests — always `await act(async () => ...)`. Future frontend tasks should run `npm test` alongside tsc/lint/build.
- 2026-07-07 — Connectors epic built (`requirements-specs/connector_tasks/`, tasks #1+#3–#7; full detail in that INDEX's progress log). New: `connection_service.py`, `connector_catalog.py` (types registry + validation + `redact_config`), `connector_tasks.py` (beat health checks, rate-limited), `TestConnectionResult` diagnostics across `base.py` + all 5 drivers, discover/restore/hard-delete endpoints, `tests/connectors/` (69 tests). Critical hotfix folded in: `GET /connectors/` was unauthenticated and returned plaintext passwords — all connector routes now auth-gated, all responses redact secrets. Verified: pytest 260/260, frontend tsc/build/vitest clean (lint 29 vs 30 baseline), live curl pass on every new endpoint after image rebuild. **Uncommitted.** Gotchas: (1) live dev Postgres needed manual additive `ALTER TABLE connections ...` (create_all doesn't alter existing tables) — SQL recorded in the connector INDEX progress log; (2) `docker exec` without `-i` silently ignores heredoc stdin — the first "migration" run was a no-op that reported success; (3) partial unique index (`sqlite_where`/`postgresql_where`) is how soft-delete + unique-name coexist, plain `unique=True` on the column must be removed; (4) seeded admin login is `admin@dataplane.ai` / `ADMIN_DEFAULT_PASSWORD`.
- 2026-07-07 — User report "Schema Mapper shows 28 pending AI suggestions on a published mapping; Accept 409s" fixed as a suggestion-lifecycle gap. Product call: **no historical suggestions on published mappings**. (a) `MappingService.publish` now supersedes all pending `ai_suggestions` in the same transaction (new status `superseded`; audit payload gains `suggestions_superseded`); (b) `suggest_mappings_task` re-runs now skip targets with a pending suggestion and never re-offer a rejected (source→target) pair (filtered before best-match selection so the next-best source can still win); (c) mapper `page.tsx` renders `SuggestionPanel` only when `mapping.status === "draft"` — it was the only panel gating on role alone, not `canEdit`. One-time SQL backfill superseded the 28 zombies (actually 14 columns × 2 duplicate task runs; `decided_by='backfill:publish-supersede'`). Verified: pytest 264/264 (4 new, incl. worker-task tests in `tests/mapping/test_suggest_task.py` using a no-close session proxy), vitest 35/35, tsc/build clean, lint 29 baseline, images rebuilt + live curl (0 pending, accept still 409s). **Uncommitted.**
