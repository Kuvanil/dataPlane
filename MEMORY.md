# MEMORY — Cross-Session Project State

This is a working log for agents (Kimchi and others) that don't retain context between sessions. Read the **Current state** section and the last 3-5 **Log** entries before starting work; append one short entry when you finish a task. This is not a changelog for humans — `git log` is authoritative for that. Keep entries short and factual; prune stale ones instead of letting this file grow unbounded (target: keep it under ~150 lines — archive older log entries into `requirements-specs/**/INDEX.md` progress logs where they already live, don't duplicate).

## Current state (as of 2026-07-06)

- **Stack:** FastAPI + Celery + Postgres backend, Next.js (App Router, React 19) frontend, Docker-first. No migration tool — schema changes apply via `Base.metadata.create_all` on app start (see `SKILLS.md` §2).
- **Core platform capabilities shipped:** Audit Trail, Schema Drift Detection (Celery beat), persistent Query History/Chat Sessions, JWT auth, AI Autopilot backend (7-step agent loop). See `backend/app/models/` and `backend/app/api/routers/` for the concrete surfaces.
- **Schema Mapper TRD gap-closure** (`requirements-specs/mapper_tasks/INDEX.md`): tasks #1 (N:1 mapping UI), #3 (nullability display), #5 (autosave-loss warning), #6 (rename UI) are done. #2 (keyboard a11y for drag-and-drop) and #4 (canvas virtualization) are open — both need a human decision (accessibility audit; virtualization library choice) before auto-implementing. #7 (tenant isolation sign-off) is blocked on product.
- **Pipelines epic** (`requirements-specs/Pipelines_tasks/INDEX.md`): tasks defined (data model, drift validation, execution engine, scheduler, retry handling, run history, UI monitoring, audit/role gating, concurrency, tests, secret vaulting) — check that INDEX.md for current status before assuming any are done.
- **Governance files added 2026-07-06:** root `CLAUDE.md` (entrypoint), `SKILLS.md` (task playbooks), this file. `prompts/*.md` remains the binding coding-standards contract and was intentionally left unmodified in that pass.
- **Dashboard epic** (`requirements-specs/dashboard_tasks/INDEX.md`): tasks #1–#7 done 2026-07-07 (aggregation API + cache + role-scoping + widget framework + tiles/feed/range-filter); #8 backend tests done, frontend component tests blocked on missing jest/RTL infra (human call); #9 tenant sign-off blocked on product.
- **Connectors epic** (`requirements-specs/connector_tasks/INDEX.md`): tasks #1, #3–#7 done 2026-07-07 (soft-delete data model + ConnectionService, types catalog + config validation, test diagnostics w/ 5s timeout across all 5 drivers, beat health checks, discovery handoff, dependency-aware delete/restore/hard-delete; plus a hotfix — connector reads were unauthenticated and returned plaintext passwords; now auth-gated + secret-redacting). #2 (secret vaulting) and #8 (edit/rotate) blocked on encryption-approach sign-off; #10 tenant sign-off blocked on product. 6/9 FRs done.

## Known constraints / gotchas

- `SECRET_KEY` (JWT) must be overridden via env var in production — code default is dev-only.
- No Alembic-equivalent migrations — destructive schema changes (drop/rename column) need a human sign-off, not an auto-implementation.
- Celery beat requires its own `beat` service entry in `docker-compose.yml` — periodic tasks silently won't fire without it.
- `frontend/src/lib/api.ts` is the single chokepoint for HTTP calls and 401 handling (`setUnauthorizedHandler`) — don't add a parallel fetch or auth-redirect path.

## Log

- 2026-07-06 — Added `CLAUDE.md`, `SKILLS.md`, `MEMORY.md` at repo root to formalize agentic-SDLC practice on top of `prompts/*.md`, and wrote instructions in a compact, section-scoped style for Kimchi's limited context window. `prompts/*.md` left as-is per decision to treat it as already current.
- 2026-07-06 — Code review of pipelines commit `3866c7e` (Tasks #1+#2) filed and **fixed** bugs #12–#19 (`requirements-specs/Pipelines_tasks/1*.md`). Highlights: the Task #1 refactor had silently destroyed the legacy `POST /execute` executor (restored verbatim + regression tests); the Pipelines INDEX had marked spec-only tasks as done FRs (corrected — real status is 2/10 FRs done); drift check hardened (fail-closed on missing snapshot, column-order-normalized hashing, column-level `changed_tables`, mapping↔connection match enforced at create). Suite: 164/164. Lesson recorded in INDEX legend: `[x]` = landed tested code only, never "spec written". Uncommitted — working tree also holds unrelated staged schema-catalog work.
- 2026-07-06 — User report "schema mapper always shows same tables / no dropdown / static" traced to (a) the **Visualize** tab auto-picking hardcoded connector ids 1/2 with unlabeled selects, and (b) the **dashboard home** being mostly string literals (tiles, activity feed, connection-health %s — only drift alerts were real). Filed + fixed 5 tasks in `requirements-specs/dashboard_static_ui_tasks/INDEX.md`: Visualize now defaults to first-two connectors with labeled 📤/📥 pickers and a proper empty state (was: infinite spinner when <2 connectors); home tiles/feed/health now driven by `GET /connectors/`, `POST /connectors/{id}/test`, `GET /audit/`, `GET /audit/summary`, `GET /mappings/` — unbacked numbers ("Total Tables 15", "AI Matches 92%", "PII 8", health %s) removed per the no-mock-UI rule. tsc/lint/build clean (lint = 30-problem baseline, zero new), frontend image rebuilt + smoke-checked live. **Uncommitted.** Gotcha for next time: seeded demo data uses connector ids 1/2 — magic-id assumptions hide in `query-studio/page.tsx` and `askdata` too (not audited this pass).
- 2026-07-06 — Review round 2 of the schema-mapper epic (same treatment as pipelines): 12 findings filed and **all fixed** (`requirements-specs/review_schema_mapper_round2/INDEX.md` has the full per-bug log). Notable: two HIGHs were regressions introduced by round-1 fixes themselves — the one-at-a-time autosave drain reintroduced a concurrency race the old `splice(0)` was immune to (fixed with a ref-based in-flight guard; state-based guards are useless inside mount-once-effect closures), and the N:1 staging set survived mapping switches. Backend gained `uq_field_target_per_draft` partial unique index (drafts weren't covered by `uq_field_target_per_version` — NULL version_ids are distinct). Verification: pytest 167/167 (3 new), tsc/build clean, lint byte-identical to HEAD baseline. **Uncommitted.** Lesson: post-review fix batches need their own review pass — both HIGHs came from the "fixes", not the original features.
- 2026-07-07 — Dashboard epic built (`requirements-specs/dashboard_tasks/`, tasks #1–#7 + #8-backend; full detail in that INDEX's progress log). New: `GET /api/v1/dashboard/summary?range=` (8 KPI tiles + enriched feed, per-module failure isolation, per-user 30s TTLCache, viewer role masking), frontend `DashboardWidget`/`KPITile`/`ActivityFeed`/`TimeRangeFilter` + `useWidgetData`, `page.tsx` rewired to the aggregation API. New dep `cachetools`. Verified: pytest 191/191 (24 new), lint at 30-problem baseline, api+frontend images rebuilt and endpoint exercised live. **Uncommitted.** Gotchas worth remembering: (1) per-module try/except MUST `db.rollback()` in each handler or one failed query poisons the rest of the session; (2) task-spec pseudocode drifted from real models (`Pipeline` has no `status`, `DBConnection` has no `is_deleted`) — always re-ground specs against `app/models/` before implementing; (3) frontend has NO jest/RTL infra — dashboard spec assumed it exists, component tests need a human decision.
- 2026-07-07 — Dashboard follow-up (user-approved): added the repo's first frontend test infra — Vitest + RTL per the Next-bundled vitest guide (`frontend/vitest.config.mts`, `vitest.setup.ts`, `npm test`); 35 component/hook tests in `src/app/dashboard/__tests__/`, all green; dashboard_tasks #8 now fully [x]. Backend dashboard coverage measured at 90% (pytest-cov in venv only, not pinned). Seeded idempotent demo data via api container (tag: actor/created_by `demo-seed`) so all 8 KPI tiles show range-scoped values — dev Postgres only, delete by that tag. **Uncommitted.** Gotcha: with React 19, `act(() => someAsyncFn())` without await corrupts the act scope and nulls `result.current` in LATER tests — always `await act(async () => ...)`. Future frontend tasks should run `npm test` alongside tsc/lint/build.
- 2026-07-07 — Connectors epic built (`requirements-specs/connector_tasks/`, tasks #1+#3–#7; full detail in that INDEX's progress log). New: `connection_service.py`, `connector_catalog.py` (types registry + validation + `redact_config`), `connector_tasks.py` (beat health checks, rate-limited), `TestConnectionResult` diagnostics across `base.py` + all 5 drivers, discover/restore/hard-delete endpoints, `tests/connectors/` (69 tests). Critical hotfix folded in: `GET /connectors/` was unauthenticated and returned plaintext passwords — all connector routes now auth-gated, all responses redact secrets. Verified: pytest 260/260, frontend tsc/build/vitest clean (lint 29 vs 30 baseline), live curl pass on every new endpoint after image rebuild. **Uncommitted.** Gotchas: (1) live dev Postgres needed manual additive `ALTER TABLE connections ...` (create_all doesn't alter existing tables) — SQL recorded in the connector INDEX progress log; (2) `docker exec` without `-i` silently ignores heredoc stdin — the first "migration" run was a no-op that reported success; (3) partial unique index (`sqlite_where`/`postgresql_where`) is how soft-delete + unique-name coexist, plain `unique=True` on the column must be removed; (4) seeded admin login is `admin@dataplane.ai` / `ADMIN_DEFAULT_PASSWORD`.
- 2026-07-07 — User report "Schema Mapper shows 28 pending AI suggestions on a published mapping; Accept 409s" fixed as a suggestion-lifecycle gap. Product call: **no historical suggestions on published mappings**. (a) `MappingService.publish` now supersedes all pending `ai_suggestions` in the same transaction (new status `superseded`; audit payload gains `suggestions_superseded`); (b) `suggest_mappings_task` re-runs now skip targets with a pending suggestion and never re-offer a rejected (source→target) pair (filtered before best-match selection so the next-best source can still win); (c) mapper `page.tsx` renders `SuggestionPanel` only when `mapping.status === "draft"` — it was the only panel gating on role alone, not `canEdit`. One-time SQL backfill superseded the 28 zombies (actually 14 columns × 2 duplicate task runs; `decided_by='backfill:publish-supersede'`). Verified: pytest 264/264 (4 new, incl. worker-task tests in `tests/mapping/test_suggest_task.py` using a no-close session proxy), vitest 35/35, tsc/build clean, lint 29 baseline, images rebuilt + live curl (0 pending, accept still 409s). **Uncommitted.**
